# Vert.x Webservices (vertx-webservices)

Basic example of using Vert.x with Kotlin to setup a HTTP server, which provides webservices via REST. To run the example execute the following command in a terminal:
    **./gradlew run**
